vendor/
!src/Providers/*/assets/**/vendor/
node_modules/

# Compiled assets
dist/

# Lock files — package-lock.json is committed so `npm ci` works on deploy.
composer.lock

# Editor / OS
.idea/
.DS_Store
.phpunit.result.cache
