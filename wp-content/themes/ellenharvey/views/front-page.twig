{% extends "base.twig" %}

{# The home page is just another page on the stage — its block content is a
   core/group (background hero photo) wrapping an ix/content-slider of
   crossfading press pull-quotes, vertically centred. base.twig renders it;
   the slider styling lives in _press-quotes.scss. #}
