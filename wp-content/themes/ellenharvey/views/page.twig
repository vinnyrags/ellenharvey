{% extends "base.twig" %}

{# Every page renders its own block content on the stage (base.twig /
   layout/_frame.scss): a core/group with a background photo wrapping a content
   panel column. There's no page-specific chrome — the design lives in the
   blocks, so Ellen edits each page's look in the editor. #}
